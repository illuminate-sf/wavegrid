# @wavegrid/programs

58 animation programs for wavegrid, using the host ABI for distributed rendering. Each program implements `render(S, ctx, frame)` with optional `init()` and `step()`, operating on a 1-D RGB framebuffer via render sets.
