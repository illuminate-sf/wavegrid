# @wavegrid/cloud-relay

Cloud relay server for wavegrid — provides login UI, pattern gallery with live previews, and token-gated WebSocket relay for forwarding commands to wavegrid agents. Replaces the old `@wavegrid/simulator` + `@wavegrid/relay` packages.
