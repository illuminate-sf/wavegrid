# DEPRECATED

`@wavegrid/relay` is deprecated and replaced by `@wavegrid/cloud-relay`.

The simple WS message router has been superseded by the cloud relay which provides authentication, pattern gallery UI, and token-gated agent WebSocket connections.
