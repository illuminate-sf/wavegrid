**/node_modules/
**/.DS_Store
**/dist
**/pnpm-debug.log
lerna-debug.log
**/.next
**/out

# local env / secrets (keep the server IP out of git) — .env.example is tracked
**/.env
**/.env.local

# user credentials (real passwords — .users.example is tracked)
.users

# persisted server state (per-instance, auto-generated)
.state/

# generated machine config (holds real LAN/cloud IPs)
deploy/routing.json
deploy/light-map.json

# Traefik runtime files (generated / contains secrets)
deploy/traefik/dynamic.toml
deploy/traefik/acme.json
